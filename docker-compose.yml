version: '3'


services:
  php:
    container_name: php
    build:
      context: ./php
      dockerfile: Dockerfile
    volumes:
      - ./upload:/app/upload
    ports:
      - "80:80"
    restart: always